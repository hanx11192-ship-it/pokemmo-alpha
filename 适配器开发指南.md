# 适配器（数据源）开发指南

> 适用对象：要给 Pokemmo Alpha 监控面板**新增一个数据源**，或把**自己写的爬虫**接进面板的人。  
> 读完这份文档，你应该能独立写一个新适配器、把爬虫转成适配器、并验证它是否正常推送。

---

## 一、先理解：面板推送消息是怎么来的

面板推送**不是单个模块**，而是五层协作产出的。适配器处在最上游，负责"把源站数据变成统一结构"。

```
src/sources/*.py  ──► BossData ──► main.resolve_by_vote（多源投票）
                                         │
                                         ▼
                            strategy/engine.generate_report（渲染文本）
                                         │
                          panel/dispatchers/default_dispatcher（决策器，可插拔）
                                         │
                                         ▼
                            panel/channels.send_all ──► 微信/Webhook/Server酱
```

各层职责一句话：

| 层  | 模块                                            | 职责                                     |
| -- | --------------------------------------------- | -------------------------------------- |
| 采集 | `src/sources/*.py` + `base.py`                | 拉源站、解析、**归一化成 `BossData`**             |
| 核心 | `src/core/models.py`、`dedup.py`、`src/main.py` | `BossData` 定义、去重、多源投票裁决                |
| 渲染 | `src/strategy/engine.py`                      | 把 `BossData` 渲染成推送文本（信息段 + 打法段）        |
| 决策 | `panel/dispatchers/*.py`、`panel/scheduler.py` | 可插拔"决策器"，默认调用渲染引擎；调度轮询                 |
| 渠道 | `panel/channels.py`                           | 真正发出去（wxpusher / webhook / serverchan） |

**本文只讲第一层——适配器怎么写。**

> 关键认知：**适配器做三件事**
>
> 1. 提供通用必要信息（`name`/`ability`/`moves` + 各 canonical id），既是打法决策键、也是信息段原料；
> 2. 提供可选追加信息（`extra_lines`：票数、剩余时间等）和"时段双出口"（`period` 进正文首行、`slot_name` 当推送摘要）；
> 3. 提供溯源与去重依据（`source`/`pokedex_id`/`period`）。

---

## 二、核心契约：你必须了解的三种结构

### 2.1 `BossData`（归一化后的头目信息）

定义在 `src/core/models.py`，是所有源和引擎之间的**唯一契约**。不管上游 API 长什么样、返回什么语言，适配器都必须把数据捏成这个结构。

```python
@dataclass
class BossData:
    # ---- 必需：引擎渲染真正依赖的全部内容 ----
    name: str                       # 头目名称（建议归一化为官方中文）
    ability: str                    # 特性（建议归一化为官方中文）
    moves: List[str] = []           # 四个技能（建议归一化为官方中文）

    # ---- 展示用可选信息（缺了不影响出招，只是推送少几行）----
    period: str = ""                # 时段窗口，如 "凌晨头(次日02:00-08:00)"
    location: str = ""             # 中文地点，如 "神奥 · Route 205"
    location_en: str = ""          # 英文地点（源提供时填，否则英文推送会夹中文）
    reporter: str = ""             # 报点人
    gender: Gender = field(default_factory=Gender)        # 性别比（male_percent）
    egg_groups: List[str] = []     # 中文蛋组
    egg_groups_en: List[str] = []  # 英文蛋组
    extra_lines: List[ExtraLine] = []   # 附加信息行（票数/剩余时间/秘传技等）

    # ---- 解析副产物：canonical id，引擎做规则匹配用 ----
    pokedex_id: Optional[int] = None     # 图鉴号
    ability_id: Optional[int] = None     # 特性 id
    move_ids: List[Optional[int]] = []   # 技能 id 列表

    # ---- 溯源 ----
    source: str = ""               # 来源名
    reported_at: str = ""          # 报点时间（ISO），用于多源投票与去重分桶
```

`Gender` 与 `ExtraLine`：

```python
@dataclass
class Gender:
    male_percent: Optional[float] = None   # 雄性百分比；None = 无性别
    # is_dual 属性：同进化链存在异性才 True（决定要不要出打法）

@dataclass
class ExtraLine:
    zh: str
    en: str
    def text(self, lang): return self.en if lang == "en" else self.zh
```

### 2.2 `FetchResult`（适配器 `fetch()` 的返回值）

```python
@dataclass
class FetchResult:
    status: str          # "hit" / "empty" / "error" 三选一
    boss: Optional[BossData] = None
    dedup_key: str = ""      # 去重标识（通常时段/报点时间）
    slot_name: str = ""      # 时段名（早头/午头/晚头/凌晨头），作推送摘要
    slot_name_en: str = ""   # 时段名英文
    message: str = ""
```

**三种状态务必区分清楚：**

| status  | 含义                | 主流程处理              |
| ------- | ----------------- | ------------------ |
| `hit`   | 拿到有效头目，应该推送       | 进入投票/渲染/发送         |
| `empty` | 请求成功，但当前时段确实没头目   | 正常，记日志，不推送         |
| `error` | 请求失败 / 解析失败 / 没查成 | 记 error 日志，可能换下一个源 |



> ⚠️ **不要把 `empty` 当 `error`**。混在一起会让日志全是噪音，也影响"该源是否健康"的判断。

### 2.3 `BaseSource`（基类现成能力，别自己造轮子）

定义在 `src/sources/base.py`。子类**只需实现 `fetch()`**，其余按需覆写。构造时自动注入：`options`（yaml 配置）、`pokedex`（图鉴）、`logger`、`config`、`sample_data`。

基类已提供的能力：

| 方法                                          | 作用                                                                     |
| ------------------------------------------- | ---------------------------------------------------------------------- |
| `http_get(url, headers)`                    | 带超时/代理的 GET，返回 `requests.Response`                                     |
| `request_json(url, headers)`                | 带重试的 JSON 请求（读 yaml 的 `retry_delays`），失败抛 `SourceError`                |
| `build_url()`                               | 拼接最终地址（配了中转走中转，否则直连 `target`）                                          |
| `resolve_entry(zh, en, kind)`               | 把任意语言名字解析成 `(canonical中文名, id)`，`kind∈{move,ability,pokemon}`，**英文优先** |
| `make_extra(label_zh, label_en, values_zh)` | 把一组中文值渲染成 `ExtraLine`（技能类自动翻译英文）                                       |
| `dedup_key(result)`                         | 去重标识；源没填则按内容算 MD5 指纹兜底                                                 |
| `log(msg, level)`                           | 带 `[源名]` 前缀的日志                                                         |
| `parse_male_ratio(value)`（模块级函数）            | 防御式解析雄性百分比（"50%"、"0.5"、"公1母7" 都能解析）                                    |
| `transform_plan()` / 中转                     | 海外源站走国内中转（`transform_url_env` + `x-proxy-key`）                         |

> 海外源站直连不通时，优先在 yaml 里加 `proxy: http://127.0.0.1:8899`（阿里云隧道），而不是自己写中转逻辑。

---

## 三、添加新适配器的标准七步

### 第 1 步：新建文件

在 `src/sources/` 下新建 `<adapter>.py`，例如 `mycrawler.py`。文件名（去掉 `.py`）就是 yaml 里要填的 `adapter` 值。

### 第 2 步：定义类与 `name`

```python
from ..core.models import BossData, FetchResult, Gender, ExtraLine
from .base import BaseSource

class MycrawlerSource(BaseSource):
    name = "mycrawler"   # 必须和文件名、yaml 的 adapter 一致
```

> 加载机制：`src/sources/__init__.py` 的 `load_source_class` 会 `importlib.import_module(f".{adapter}", ...)`，再找其中 `BaseSource` 的子类实例化。所以**文件名 == `adapter` 字段 == 类里 `name`** 三者要对齐。

### 第 3 步：实现 `fetch() -> FetchResult`

这是唯一必须实现的方法。骨架：

```python
def fetch(self) -> FetchResult:
    # 1) 离线样本注入（测试用，可选）
    if self.sample_data is not None:
        raw = self.sample_data
    else:
        try:
            raw = self._crawl()        # 你的抓取/爬虫逻辑
        except Exception as e:
            return FetchResult("error", message=f"抓取失败: {e}")

    if not raw:
        return FetchResult("empty", message="当前时段没有头目")

    try:
        boss = self._to_bossdata(raw)  # 原始字段 -> BossData
    except Exception as e:
        return FetchResult("error", message=f"解析失败: {e}")

    boss.source = self.name
    slot_name = boss.period.split("(")[0]   # 取 "凌晨头" 作摘要
    return FetchResult("hit", boss=boss, slot_name=slot_name,
                       message=f"命中: {boss.name}")
```

### 第 4 步：写转换逻辑（原始字段 → `BossData`）

这是适配器的核心。重点：

- **名称/特性/技能优先用英文查 `resolve_entry(en=..., kind=...)`**，拿到 canonical 中文名 + id，能绕开机翻。
- 源不给特性时，头目一律隐藏特性，从图鉴 `self.pokedex.pokemon[str(pid)]["hidden_ability"]` 补。
- 英文地点给 `location_en`；中文地点给 `location`。
- 票数、剩余时间等额外信息，放进 `extra_lines`（见第五章）。

### 第 5 步：（可选）自定义请求头 / 代理 / 中转

```python
HEADERS = {
    "User-Agent": "Mozilla/5.0 ... Chrome/124.0 Safari/537.36",
    "Referer": "https://源站/",
}
```

海外源站在 yaml 配 `proxy` 即可，基类 `http_get` 会自动用它。

### 第 6 步：注册到 `config/sources.yaml`

照抄一段，填 `adapter` 名：

```yaml
sources:
  - name: 我的爬虫源          # 面板显示名（可任意）
    adapter: mycrawler        # 必须 == 文件名(无.py) == 类的 name
    enabled: true
    priority: 15              # 数字越小越优先（多源投票平票时兜底）
    options:
      target: https://...     # 若用 self.http_get / request_json 抓，填这里
      proxy: http://127.0.0.1:8899   # 海外源站才需要隧道代理
      extra_lines:
        level: true
        vote: true
```

> 停用把 `enabled` 改成 `false`；调优先级改 `priority`。**主流程代码一行都不用改。**

### 第 7 步：验证（见第六章）

---

## 四、常见字段映射速查表

把你手上/爬虫拿到的字段，对照填到 `BossData`：

| 你/爬虫有的          | 放哪                                                                | 备注               |
| --------------- | ----------------------------------------------------------------- | ---------------- |
| 头目英文名           | `resolve_entry(en=..., kind="pokemon")` → `name`(+`pokedex_id`)   | 英文优先查 canonical  |
| 头目中文名           | `resolve_entry(zh=..., kind="pokemon")`                           | 无英文时退回           |
| 特性              | `resolve_entry(en/zh, kind="ability")` → `ability`(+`ability_id`) | 缺则补图鉴隐藏特性        |
| 4 个技能           | `resolve_entry(en/zh, kind="move")` → `moves`(+`move_ids`)        | 取前 4 个           |
| 地区/地点           | `location`（中）、`location_en`（英）                                    | 海外地名保留英文         |
| 性别比             | `Gender.from_male_percent(50.0)`                                  | 无性别传 `None`      |
| 蛋组              | `egg_groups`（中）、`egg_groups_en`（英）                                | 可省               |
| 报点人             | `reporter`                                                        | 可省               |
| **票数、剩余时间、秘传技** | `extra_lines`（ExtraLine）                                          | **可选追加消息**，不参与打法 |
| 时段窗口            | `period`（正文首行）+ `slot_name`（摘要）                                   | 两个出口都给           |
| 图鉴号             | `pokedex_id`                                                      | 供去重与投票           |
| 报点时间            | `reported_at`（ISO 字符串）                                            | 供投票裁决            |

---

## 五、实战：把"自己的爬虫"转成适配器

假设你写了一个爬虫，能拿到：头目必要信息 + 一些其他信息（**票数**、**剩余时间**）。下面按"从零接进去"讲。

### 5.1 把爬虫逻辑放进 `_crawl()`（三种姿势）

**姿势 A：爬虫逻辑直接写在适配器里（最干净）**

用基类现成的 `http_get`，自动带重试/超时/代理：

```python
def _crawl(self) -> Optional[dict]:
    url = self.options.get("target")
    resp = self.http_get(url, self.HEADERS)
    resp.raise_for_status()
    return resp.json()   # 返回下面的 dict 结构
```

**姿势 B：爬虫是独立脚本，subprocess 调用**

```python
import subprocess, json
def _crawl(self) -> Optional[dict]:
    out = subprocess.check_output(["python3", "/path/your_crawler.py"])
    return json.loads(out)
```

**姿势 C：爬虫跑在别处，把结果写文件/DB，适配器读**

```python
import json
def _crawl(self) -> Optional[dict]:
    with open("/tmp/crawler_result.json", encoding="utf-8") as f:
        return json.load(f)
```

### 5.2 票数、剩余时间 → `extra_lines`（可选追加消息）

**这是你之前总结的"可选追加消息"的标准落位**。引擎不依赖它们，只会在推送里追加几行：

```python
extra: List[ExtraLine] = []
if raw.get("votes") is not None:
    extra.append(ExtraLine(zh=f"票数: {raw['votes']}", en=f"Votes: {raw['votes']}"))
if raw.get("remaining_seconds") is not None:
    rem = int(raw["remaining_seconds"])
    dur = f"{rem // 60} 分钟" if rem >= 60 else f"{rem} 秒"
    extra.append(ExtraLine(zh=f"剩余消失: 约 {dur}", en=f"Despawns in ~{dur}"))
# 然后 BossData(..., extra_lines=extra)
```

> 如果额外值是"技能类"列表（会自动翻英文），可以用基类 `make_extra("秘传技需求", "HMs required", ["Surf"])` 更省事。

### 5.3 时段 / 推送摘要怎么定

"时段窗口"和"剩余时间"是**两回事**，别混：

- `period`：`凌晨头(次日02:00-08:00)` 这种**时段窗口**，进推送**正文首行**，也参与去重分桶。
- `slot_name`：`凌晨头` 这种短名，作**推送摘要**（WxPusher 通知标题/预览）。
- 剩余时间是**额外上下文**，放 `extra_lines`，不决定时段。

按北京时间算时段（与 pokemmotools 源一致）：

```python
SLOTS = [
    ("凌晨头", "次日02:00-08:00", lambda h: 2 <= h < 8),
    ("早头",   "08:00-14:00",     lambda h: 8 <= h < 14),
    ("午头",   "14:00-20:00",     lambda h: 14 <= h < 20),
    ("晚头",   "20:00-次日02:00", lambda h: 20 <= h or h < 2),
]
@staticmethod
def _slot_of(hour):
    for name, rng, pred in SLOTS:
        if pred(hour): return name, rng
    return "晚头", "20:00-次日02:00"

# fetch 里：
slot_name, slot_rng = self._slot_of(datetime.now(CN_TZ).hour)
boss.period = f"{slot_name}({slot_rng})"
FetchResult("hit", boss=boss, slot_name=slot_name, ...)
```

> 若你的爬虫/源本身就有时段或起止时间，直接用它的；没有才按北京时间算。

### 5.4 去重不用你管

`src/core/dedup.py` 用 **图鉴号 + 时段** 自动算去重键（`global_dedup_key(boss)`）。只要你在 `BossData` 里正确填了 `pokedex_id` 和 `period`，lzpoke / pokemmotools / 你的源就不会重复推同一条头目。只有"没有时段概念"的源才需要在 `FetchResult.dedup_key` 里自己给标识。

### 5.5 完整模板

可直接复制为 `src/sources/mycrawler.py` 的参考模板已随本文提供（见同目录 `mycrawler.py`）。它的结构就是：

```
MycrawlerSource(BaseSource)
 ├─ name = "mycrawler"
 ├─ fetch()            # 调 _crawl() → _to_bossdata() → 返回 FetchResult("hit")
 ├─ _crawl()          # ★ 你的爬虫逻辑（替换 NotImplementedError）
 └─ _to_bossdata()    # 原始 dict → BossData（名称/特性/技能解析 + extra_lines + 时段）
```

---

## 六、验证与调试

### 6.1 离线样本注入（不发网络请求）

适配器复用了 `sample_data` 注入模式：`if self.sample_data is not None: raw = self.sample_data`。  
把一份爬虫返回样例通过 `sample_data` 传进去，就能在不依赖网络/爬虫的情况下验证"解析 + 渲染"是否正确。

本地快速自测脚本（在 VPS 项目目录下跑）：

```python
import sys; sys.path.insert(0, "/opt/pokemmo_alpha")
from src.sources.mycrawler import MycrawlerSource
from src.core.pokedex import get_pokedex

sample = {
    "pokemon": "Luxray", "ability": "Guts",
    "moves": ["Thunderbolt","Superpower","Facade","Signal Beam"],
    "region": "Sinnoh", "location": "Route 205",
    "votes": 12, "remaining_seconds": 171, "reporter": "@x",
    "gender_rate": 50.0,
}
src = MycrawlerSource(pokedex=get_pokedex(), sample_data=sample)
res = src.fetch()
print(res.status)
print(res.boss)
```

### 6.2 面板"debug 立即检查"

面板 → 调度页 → **debug 立即检查**：跑一轮只生成报告、不推送、不写去重标记。看渲染出的完整消息，确认格式、中文地区、时段命名（凌晨头/早头/午头/晚头）是否符合预期。

### 6.3 日志排查

- 源报 `empty`：正常，说明"查了没头目"。
- 源报 `error`：查 `message`（如 `HTTP 403`、`JSON 解析失败`、`爬虫失败: ...`）。
- 多源都 `hit`：看面板"日志"页的 `query` 类，确认每个源的返回与投票裁决结果。

---

## 七、常见坑（避坑清单）

1. **`adapter` 字段对不上文件名**：`adapter: mycrawler` 必须对应 `src/sources/mycrawler.py`，且类 `name = "mycrawler"`。三者不一致会 `ImportError: 模块里没有找到 BaseSource 的子类`。
2. **名字解析英文优先**：源给英文就用 `resolve_entry(en=...)`，给中文才 `resolve_entry(zh=...)`。混用机翻中文会查不到 id，导致打法匹配失效。
3. **特性别留空**：Pokemmo 头目一律隐藏特性，源不给时从图鉴 `hidden_ability` 补。
4. **`empty` ≠ `error`**：前者是"查了没有"，后者是"没查成"。
5. **海外源站需隧道代理**：VPS 直连海外不通，yaml 加 `proxy: http://127.0.0.1:8899`（阿里云 autossh 隧道），别自己写中转。
6. **`monster_id` 除数**：有的源给的是内部 id，需 ÷N 才得全国图鉴号，除数写在 yaml 配置里，别硬编码。
7. **票数/剩余时间是"附加信息"**：放 `extra_lines`，不是 `moves`/`ability` 那种必要字段，别塞错地方导致引擎误判。
8. **时段双出口**：`period`（正文首行）和 `slot_name`（摘要）都给最稳；只给一个也行，但摘要缺失时 WxPusher 会显示兜底文字"头目"。

---

## 八、参考文件

| 文件                                    | 作用                                                     |
| ------------------------------------- | ------------------------------------------------------ |
| `src/sources/base.py`                 | `BaseSource` 基类，现成的请求/解析/去重能力                          |
| `src/core/models.py`                  | `BossData` / `FetchResult` / `Gender` / `ExtraLine` 定义 |
| `src/sources/__init__.py`             | `create_source` / `load_source_class` 动态加载逻辑           |
| `src/sources/lanbizi.py`              | 带详细注释的经典模板（字段缺失处理套路）                                   |
| `src/sources/pokemmotools_landing.py` | 公开页 + 图鉴补技能/特性的实战范例                                    |
| `config/sources.yaml`                 | 数据源注册表，加新源照抄一段                                         |
| **`mycrawler.py`（同目录）**               | **本文配套的爬虫转适配器完整模板**                                    |
