# Alpha 面板环境变量（可在面板「系统配置」页修改）
# Alpha 面板环境变量（可在面板「系统配置」页修改）
# Alpha 面板环境变量（可在面板「系统配置」页修改）
# 在此填写环境变量（按需，留空则使用默认值）
# Alphapedia 转发服务地址（VPS 直连海外不通时需配置，否则定时轮询拿不到数据）
# 已废弃:旧 PHP 中转(trans/aby 均未备案,出站被 IDC 拦截)不再使用。
# LZPoke 现改为源级隧道代理,见 config/sources.yaml 的 proxy 配置。
NO_PROXY=127.0.0.1,localhost
# WxPusher 推送 token（用于头目播报推送）
WXPUSHER_APP_TOKEN_alpha=
PROXY_KEY=
